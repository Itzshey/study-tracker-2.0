{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run StudyTracker",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/studyTracker.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb.exe",
            "preLaunchTask": "build-studytracker",
            "console": "integratedTerminal"
        }
    ]
}